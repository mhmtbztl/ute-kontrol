'use strict';
const { createClient } = require('@supabase/supabase-js'); const { createRepository } = require('../core/marketing_funnel_worker_repository'); const { runFunnelFindings } = require('../core/marketing_funnel_worker_service');
function required(n) { const v = String(process.env[n] || '').trim(); if (!v) throw new Error(`${n}_REQUIRED`); return v; }
async function main() { const client = createClient(required('SUPABASE_URL'), required('SUPABASE_SERVICE_ROLE_KEY'), { auth: { persistSession: false, autoRefreshToken: false, detectSessionInUrl: false } }); const result = await runFunnelFindings(createRepository(client), { tenantId: process.env.MARKETING_TENANT_ID || null, limit: Number(process.env.MARKETING_FUNNEL_LIMIT || 200), asOfDate: new Date().toISOString().slice(0, 10) }); process.stdout.write(`${JSON.stringify(result)}\n`); if (!['COMPLETED', 'PARTIAL'].includes(result.status)) process.exitCode = 1; }
main().catch(e => { process.stderr.write(`${JSON.stringify({ status: 'WORKER_CRASHED', errorCode: String(e.code || e.message || 'UNKNOWN').slice(0, 100) })}\n`); process.exitCode = 1; });
